const { network } = require("hardhat")

!developmentChains.includes(network.name) ? SocketPendingSubscriber.describe
: describe("SoftRock unit testing", function () {
    let transactionData
    
    beforeEach(async () => {})
})